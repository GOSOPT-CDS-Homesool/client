import styled from "styled-components";
import { FooterIc } from "../../assets";

export default function PageFooter() {
  return <FooterIcon />;
}

const FooterIcon = styled(FooterIc)`
  margin: 0 0 12.5rem -1.65rem;
`;
